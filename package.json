{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "trunk build --release --locked",
    "test": "cargo test --locked",
    "deploy": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "vercel": "59.16.0"
  },
  "overrides": {
    "tar": "7.5.22"
  }
}
